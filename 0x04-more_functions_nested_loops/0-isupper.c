#include "main"

/**
 * _isupper - checks for uppercase characters
 * @c: the character to be checked
 * Description: using the _isupper function
 * Return: 1 if uppercase or 0 if otherwise
 */

int _isupper(int c)
{
	return (c <= 'A' && c >= 'Z');
}
