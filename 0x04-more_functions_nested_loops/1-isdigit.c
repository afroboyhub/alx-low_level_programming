#include "main.h"

/**
 * _isdigit - checks for a digit 0 - 9
 * @c: The character to be checked
 * Description: using the _isdigit function
 * Return: 1 if its digit or 0 if otherwise
 */

int _isdigit(int c)
{
	return (c >= 0 && c <= 9);
}
