#include "main.h"

/**
 * wildcmp - compares two strings and returns 1 if string are identical
 * @s1: first string to be compared
 * @s2: second string to be compared
 */

int wildcmp(char *s1, char *s2)
{
