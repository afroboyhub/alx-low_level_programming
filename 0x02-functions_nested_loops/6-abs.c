#include "main.h"

/**
 * _abs - computes the absolute value of an int
 * @n: the integer to be computed
 *
 * Return: the absolute value of and int
 */

int _abs(int n)
{
	if (n >= 0)
		return (n);
	else
		return (-n);
}
